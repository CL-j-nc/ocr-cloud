name: OCR Runner Sanity

on:
  workflow_dispatch:

jobs:
  sanity:
    runs-on: [self-hosted, macOS, ARM64, ocr-runner]
    steps:
      - name: sanity check
        shell: bash
        run: |
          echo "runner OK"
          uname -m
